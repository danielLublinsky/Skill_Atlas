---
name: two
description: Second fixture skill; mentions disabled and absent targets.
---

# two

Use `x` for the beta workflow, and `alpha:ghost` when it lands.

```example
This fenced `plain-skill` mention is documentation, not instruction.
```
