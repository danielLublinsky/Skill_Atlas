# Claude Code local state — untracked entirely (2026-08-14). This repo's own
# `.claude/skill-atlas/` scope is machine-local: graph, atlas, catalog shards
# and debug log are derived, and `categories.json` / `config.json` are curated
# per scope and re-bootstrap on the next /skill-atlas run.
.claude/

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# OS
.DS_Store
